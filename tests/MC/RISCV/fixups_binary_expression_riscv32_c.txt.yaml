test_cases:
  -
    input:
      bytes: [ 0x63 ]
      arch: "CS_ARCH_RISCV"
      options: [ "riscv32", "c" ]
    expected:
      insns:
        -
          asm_text: "beq a0, a1, .LBB1+32"

  -
    input:
      bytes: [ 0x01 ]
      arch: "CS_ARCH_RISCV"
      options: [ "riscv32", "c" ]
    expected:
      insns:
        -
          asm_text: "c.beqz a0, .Ltmp1-2"

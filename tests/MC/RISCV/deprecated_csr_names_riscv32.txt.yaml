test_cases:
  -
    input:
      bytes: [ 0x73, 0x10, 0x30, 0x14 ]
      arch: "CS_ARCH_RISCV"
      options: [ "riscv32" ]
    expected:
      insns:
        -
          asm_text: "csrrw zero, stval, zero"

  -
    input:
      bytes: [ 0x73, 0x10, 0x30, 0x34 ]
      arch: "CS_ARCH_RISCV"
      options: [ "riscv32" ]
    expected:
      insns:
        -
          asm_text: "csrrw zero, mtval, zero"

  -
    input:
      bytes: [ 0x73, 0x10, 0x00, 0x18 ]
      arch: "CS_ARCH_RISCV"
      options: [ "riscv32" ]
    expected:
      insns:
        -
          asm_text: "csrrw zero, satp, zero"
